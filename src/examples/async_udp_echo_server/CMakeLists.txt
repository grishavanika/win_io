if (TARGET win_socks)
    add_executable(async_udp_echo_server "main.cpp")
    set_all_warnings(async_udp_echo_server PRIVATE)
    target_link_libraries(async_udp_echo_server PRIVATE win_socks)
else()
    message("[x] No win_socks library found. Skipping async_udp_echo_server.")
endif()
